
<nav class="navbar navbar-expand-lg custom-nav-bg text-white">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <%= link_to(root_path) do %>
                    <%= image_tag "logo.png", alt: "logo", height: "60", width: "173" %>
                <% end %>
                <li class="nav-item my-auto">
                    <%= link_to "Home", home_index_path, class: "rounded nav-link text-white custom-nav-link " %>
                </li>
                <li class="nav-item my-auto">
                    <a href="#" class="rounded nav-link text-white custom-nav-link ">FAQs</a>
                </li>
                <% if user_signed_in? %>
                    <li class="nav-item my-auto">
                        <%= link_to t(".orders"), orders_path, class: "rounded nav-link text-white custom-nav-link " %>
                    </li>
                    <li class="nav-item my-auto">
                        <%= link_to t(".find_parts"), find_detail_preps_path, class: "rounded nav-link text-white custom-nav-link" %>
                    </li>
                    <li class="nav-item my-auto">
                        <%= link_to t(".new_order"), order_preps_path, class: "rounded nav-link text-white custom-nav-link" %>
                    </li>
                <%end%>
            </ul>
            <%= render partial: "locales/locale"%>
            <div class="mx-2">
                <% if user_signed_in? %>
                    <%= current_user.email %>
                <%end%>
            </div>
            <% if user_signed_in? %>
                <%= button_to t(".log_out"), destroy_user_session_path, class: "btn btn-primary mx-2", method: :delete %>
            <% else %>
                <%= link_to "Log In", new_user_session_path, class: "btn btn-primary mx-2" %>
            <%end%>
        </div>
    </div>
</nav>
